CREATE TABLE SHIPS(
SHIP_ID     NUMBER,
SHIP_NAME   VARCHAR(20),
CAPACITY    NUMBER,
LENGHT      NUMBER);

INSERT INTO SHIPS (SHIP_ID, SHIP_NAME, CAPACITY, LENGHT)
VALUES (1,'Codd Crystal', 2025, 855);

SELECT *
FROM SHIPS;

create table hola(
Nombre varchar (20)
);

create table adios(
Nombre varchar (20)
);